// Bidul Adventures game
// Main event loop

#include "INITIALIZATION/init.h"
#include "WinAPI_HANDLER/WINDOWS_HANDLER.h"

#include <windows.h>
#include <stdbool.h>
#include <stdint.h>

int main(void) {
    init();

    while(1) {
        // event loop

        //if something is pressed
        break;
    }
    return 0;
}